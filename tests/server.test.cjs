'use strict';const test=require('node:test'),assert=require('node:assert/strict'),{createServer}=require('../server.cjs');test('server serves game and rejects unknown paths',async()=>{const s=createServer();await new Promise(r=>s.listen(0,'127.0.0.1',r));const base='http://127.0.0.1:'+s.address().port;try{let r=await fetch(base+'/');assert.equal(r.status,200);assert.match(await r.text(),/쌍계/);r=await fetch(base+'/secret');assert.equal(r.status,404)}finally{await new Promise(r=>s.close(r))}});